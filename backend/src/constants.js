// Shared domain constants — import these rather than repeating the magic
// number, so limits stay consistent wherever they're checked (seed data now,
// the profile-edit endpoint once real auth exists to gate it).
export const MAX_BIO_LENGTH = 280;
